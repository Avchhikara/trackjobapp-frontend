import { Layout, Menu, Breadcrumb } from "antd";

const { Header, Content, Footer } = Layout;

export default () => {
  return (
    <Header>
      <div className="logo">Track Your Job Applications</div>
    </Header>
  );
};
